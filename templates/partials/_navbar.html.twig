<nav class="sticky top-0 z-50 bg-surface/80 backdrop-blur-xl border-b border-outline-variant/10 w-full">
    <div class="max-w-[1600px] mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <div class="flex items-center gap-8">
                <a href="/" class="text-xl font-bold tracking-tighter text-primary flex items-center gap-2">
                    <span class="material-symbols-outlined">monitoring</span>
                    Aerie Exchange
                </a>
                <div class="hidden md:flex gap-6 mt-1">
                    <a href="/" class="text-primary font-bold text-sm border-b-2 border-primary pb-1 transition-colors">Markets</a>
                    <a href="/dashboard" class="text-on-surface-variant hover:text-on-surface font-bold text-sm transition-colors pb-1 border-b-2 border-transparent hover:border-outline-variant/30">Portfolio</a>
                </div>
            </div>
            
            <div class="flex items-center gap-4">
                <div class="relative hidden sm:block">
                    <input type="text" placeholder="Search markets..." class="bg-surface-container-lowest border-none rounded-lg px-4 py-1.5 text-sm focus:ring-1 focus:ring-primary w-64 text-on-surface outline-none placeholder:text-outline-variant font-mono">
                </div>
                <button class="material-symbols-outlined text-on-surface-variant hover:text-on-surface transition-colors active:scale-95">notifications</button>
                
                {% if app.user %}
                    <a href="/dashboard" class="w-8 h-8 rounded-full bg-surface-container-high flex items-center justify-center text-primary border border-outline-variant/20 hover:bg-surface-bright transition-colors" title="Logged in as {{ app.user.userIdentifier }}">
                        <span class="material-symbols-outlined text-sm">account_circle</span>
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="material-symbols-outlined text-on-surface-variant hover:text-on-surface transition-colors active:scale-95" title="Sign In">account_circle</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>